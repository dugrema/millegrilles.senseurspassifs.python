services:
  senseurspassifs_relai:
    image: "replace_me"
    command: ["-m", "senseurspassifs_relai_web"]
    x-millegrilles-certificat:
      roles:
        - senseurspassifs_relai
      exchanges:
        - 2.prive
        - 1.public
      dns:
        localhost: true
        hostnames:
          - senseurspassifs_relai
        domain: true
    environment:
      - MQ_HOSTNAME=${MQ_HOSTNAME:-mq}
      - MQ_PORT=${MQ_PORT:-5673}
      - MG_REDIS_HOST=redis
      - MG_REDIS_PORT=6379
      - REDIS_PASSWORD_PATH=/run/secrets/passwd.redis.txt
      - KEY_PATH=/run/secrets/key_cert.pem
      - CA_PATH=/run/secrets/millegrille.pem
      - WEB_PORT=1443
      - WEBSOCKET_PORT=1444
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 150M
      restart_policy:
        condition: on-failure
        delay: 20s
        max_attempts: 1000
    configs:
      - source: mgca
        target: /run/secrets/millegrille.pem
    secrets:
      - source: senseurspassifs_relai
        target: key_cert.pem
      - source: passwd_redis
        target: passwd.redis.txt
    hostname: senseurspassifs_relai
    networks:
      - app_net

networks:
  app_net:
    name: ${INSTANCE_NAME}_net
    external: true

configs:
  mgca:
    file: ${MILLEGRILLES_ROOT}/etc/millegrille.pem

secrets:
  senseurspassifs_relai:
    file: ${MILLEGRILLES_ROOT}/secrets/senseurspassifs_relai.pem
  passwd_redis:
    file: ${MILLEGRILLES_ROOT}/secrets/redis.txt
